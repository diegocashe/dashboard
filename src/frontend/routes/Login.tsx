import { type FC } from "react";
import { LoginForm } from "../components/LoginForm";

export const Login: FC = () => {
  return(
    <section className="w-[300px] max-w-[400px]">
      <LoginForm />
    </section>
  )
} 